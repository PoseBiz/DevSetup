all:
  doctrine:
    class: sfDoctrineDatabase
    param:
      dsn:    mysql:host=localhost;dbname=pose
      username: root
      password: password

readonly:
  doctrine:
    class: sfDoctrineDatabase
    param:
      dsn:    mysql:host=localhost;dbname=pose
      username: root
      password: password
